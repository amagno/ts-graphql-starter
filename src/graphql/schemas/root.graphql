# import * from 'task.graphql'
# import * from 'user.graphql'

type Query {
  user(id: ID!): User
  users: [User]
  tasks: [Task]
}
type Mutation {
  addUser(input: UserInput): User
  addTask(userId: ID!, input: TaskInput): Task
  login(input: UserInput): Token
}